"use strict";
/*import express, { Request, Response } from "express"
import { RegisterRouters } from '.route/routes';

const app = express()
const PORT = 3040 // Verifica se ja existe alguma porta já definida no env (enviroment), se n tiver, define 3000 como padrão
app.use(express.json()) // Escolhe o tipo de arquivo pelo qual será feita a comunicação json ou xml

const apiRouter = express.Router();
RegisterRoutes()
app.listen(PORT, () => console.log(`API em execução no URL: http://localhost:${PORT}`));
*/
